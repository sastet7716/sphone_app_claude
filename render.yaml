services:
  - type: web
    name: checkbox-app
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    # Render が渡す PORT で待ち受けないとヘルスチェックに失敗します
    startCommand: gunicorn app:app --bind 0.0.0.0:$PORT
